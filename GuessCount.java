//import Main.java;

public class GuessCount {
    
    //gets count to see how many guesses the user has guessed
    public static int countNumber(int count){
        count = count + 1;
        return count;
    }

}
